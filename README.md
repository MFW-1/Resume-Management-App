Resume Management App

Frontend is made by using React.js. 
Backend is made by using Asp.Net Core Web Api including microsoft.EntityFramework. 
As a datasource SQL server is used, but it is not mandatory it can easily be changed.
